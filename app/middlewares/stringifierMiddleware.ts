// import {AnyAction, Dispatch, Store} from "redux";
// import {IAppState} from "../store";
// import {TAction} from "../modules/actions";

// export const stringifierMiddleware = (store:Store<IAppState>) => (next:Dispatch<AnyAction>) => (action:TAction) => {
  // console.group(action.type);
  // console.info('stringifierMiddleware: action:', action);
  // let result = next(action);
  // console.log('stringifierMiddleware: next state', store.getState());
  // console.groupEnd();
//   return result
// };
